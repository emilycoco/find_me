import React, { Component } from 'react';
import styles from './Footer.styl';

export class Footer extends Component {
    render() {
        return (
            <footer className="footer content-container top-margin">Copyright Emily Coco</footer>
        )
    }
}