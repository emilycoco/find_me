@require '../../../styles/styleHelpers.styl';

.dot {
    background: red;
    width: 20px;
    height: 20px;
    border-radius: 10px;
    position: absolute;
    &.marker {
      background: blue;
    }
}

@media only screen and (max-width: $max-tablet) {
}